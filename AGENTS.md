# 开发约定

本项目维护 Web 写作助手。核心流程：研究员检索 → 策划提纲 → 导师先讲再问 → 作者回答 → 编辑整理 → 终审建议。

- 保留作者自己的声音。编辑以作者回答为依据；终审提出建议，不改正文，不编造作者经历。
- `writing_assistant.py` 是共用引擎，`server.py` 负责 HTTP、SSE 和 Web 会话，`web/` 负责界面。
- `makingof.py` 导入时会切换引擎提示词，仅在启用项目复盘模式时导入。避免在学习模式中无条件导入。
- 后台当前按本地单用户、单进程串行使用设计。模型配置、提示词、警告收集和来源戳有进程级状态，不应直接扩展到并发多用户服务。
- 提示词是软约束；空输出、截断、格式错误、重试上限与文件保存要有代码保证。
- `.env`、作者回答、草稿、素材、完整评测集及原始结果留在 Git 忽略的本地目录，不作为演示数据。
- 保持 Python 和前端依赖清单与锁文件一致。公开评测样例修改后须同步指纹，不能把小样例得分当成效果证明。
- 按当前会话确认的阶段范围工作。运行检查的命令见 README；区分静态核对、替身测试与真实模型验证，不能混报结果。

可选开发模块 `fact_checker.py`、`run_eval.py`、`eval_schema.py` 与 Web 共用模型出口，但没有独立的 Web 核查页面。保留的键盘输入适配器和串行复盘驱动仅用于内部兼容与回归检查，不增加独立命令行产品功能。

Web 产品只提供真实写作。空配置时提供说明并阻止模型调用；不要重新加入演示分支。测试替身仅放在测试代码中。修改 /api/start 时保持对旧 stub 参数的拒绝，避免旧客户端意外触发付费调用。
